function characterSequence(input){

    let char = input[0];

    for (let index = 0; index < char.length; index++) {
        
        console.log(char.charAt(index));
        
    }
}

characterSequence(["ice cream"])