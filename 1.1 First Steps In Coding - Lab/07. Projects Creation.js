function projects(input) {

    let nameOfArchitect = input[0];
    let numOfProects = input[1];
    let neededHours = numOfProects * 3;

    let result = `The architect ${nameOfArchitect} will need ${neededHours} hours to complete ${numOfProects} project/s.`;

    console.log(result);

}

projects["Sanya ", "9 "];
